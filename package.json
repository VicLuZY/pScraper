{
  "name": "permit-viewer",
  "version": "1.0.0",
  "private": true,
  "description": "Static GitHub Pages permit database viewer.",
  "scripts": {
    "test": "node scripts/verify-viewer.mjs"
  }
}
